export default function PostCarousel({ posts }) {
  return (
    {/* <div className="post-carousel">
      <div className="post-carousel__container">
        <h1 className="post-carousel__title">What's New?</h1>
        <div className="post-carousel__content">
          <div className="post-carousel__list">
            {posts.map((post, index) => (
              <div className="post-carousel__item" key={index}>
                <div className="post-carousel__item-img">
                  <img src={post.image} alt={post.title} />
                </div>
                <div className="post-carousel__item-title">
                  <h5>{post.title}</h5>
                </div>
                <div className="post-carousel__item-brief">
                  <p>{post.brief}</p>
                </div>
                <div className="post-carousel__item-more">
                  <a href={post.link}>Read Article &#8594;</a>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div> */}
  );
}